# Real-World Regression Suite

Cloned rule collections from real open-source repos.
Run `node test/regression.js` before every release.

These are snapshots — we don't update them. The point is stable regression detection.
